name: sample_web
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  wellcare_checkout:
    path: ../package/

dev_dependencies:
  flutter_test:
    sdk: flutter
  linter: ^1.3.0

flutter:
  uses-material-design: true
