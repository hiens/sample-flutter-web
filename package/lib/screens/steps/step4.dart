part of '../checkout.dart';

class CheckoutStep4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ResponsiveWidget(
      childXs: ListView(
        children: <Widget>[],
      ),
    );
  }
}
